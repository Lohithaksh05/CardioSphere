import "./styles.css"

export default function DietAndFitness({className, df, alt}) {
    // make this actually functional and not just a placeholder
    return (
        <img className={className} src={df} alt={alt}/>
    )
}